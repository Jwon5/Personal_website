import { container } from "../material-kit-react.js";

const footerPage = {
  container,
  socialIcon: {
    display: "inline-block",
    textDecoration: "none",
  },
  copyRight: {
    fontSize: "1em",
    fontWeight: "200",
  },
};

export default footerPage;
